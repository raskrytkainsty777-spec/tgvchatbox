# Тест фильтра по ботам (чекбоксы)

## 🧪 Сценарий проверки

### Начальное состояние
- Откройте приложение
- В консоли (F12) должно быть:
```
Auto-selecting active bots: ["cfa0fb16-c95a-47fc-8131-456532b230cd"]
```
- В списке чатов: **2 чата** (WALEAD, Любовь)
- Чекбокс бота: **✅ выбран**

---

## Тест 1: Снять чекбокс бота

### Действие:
Кликните на чекбокс рядом с @apotestimka_bot (снять галочку)

### Ожидаемое в консоли:
```
Bot filter toggled. Selected bots: []
useEffect triggered: {selectedBots: [], ...}
No bots selected - showing empty list
```

### Ожидаемое в UI:
- ✅ Список чатов **пустой**
- ✅ Показывается сообщение: **"Выберите хотя бы одного бота"**
- ✅ Чекбокс бота: **☐ не выбран**

---

## Тест 2: Поставить чекбокс обратно

### Действие:
Кликните на чекбокс снова (поставить галочку)

### Ожидаемое в консоли:
```
Bot filter toggled. Selected bots: ["cfa0fb16-c95a-47fc-8131-456532b230cd"]
useEffect triggered: {selectedBots: Array(1), ...}
Loading chats with params: {bot_ids: "cfa0fb16-c95a-47fc-8131-456532b230cd"}
Loaded 2 chats
```

### Ожидаемое в UI:
- ✅ Список чатов: **2 чата** (WALEAD, Любовь)
- ✅ Чекбокс бота: **✅ выбран**

---

## Тест 3: Снять чекбокс + применить фильтр

### Действие:
1. Снимите чекбокс бота
2. Откройте фильтр → выберите "Важное"

### Ожидаемое в консоли:
```
Bot filter toggled. Selected bots: []
No bots selected - showing empty list
ChatList: Applying filter {type: 'label', ...}
handleFilterChange called: {type: 'label', ...}
No bots selected - showing empty list
```

### Ожидаемое в UI:
- ✅ Список пустой (нет выбранных ботов)
- ✅ Сообщение: "Выберите хотя бы одного бота"
- ✅ Чип фильтра "Важное" отображается

---

## Тест 4: Поставить чекбокс с активным фильтром

### Действие:
1. Фильтр "Важное" активен
2. Поставьте чекбокс бота

### Ожидаемое в консоли:
```
Bot filter toggled. Selected bots: ["..."]
useEffect triggered: {selectedBots: Array(1), filterType: "label", ...}
Loading chats with params: {bot_ids: "...", label_id: "..."}
Loaded 1 chats
```

### Ожидаемое в UI:
- ✅ Список: **1 чат** (WALEAD с меткой "Важное")
- ✅ Чип "Важное" отображается

---

## ✅ Итоговая проверка

| Чекбокс бота | Фильтр | Ожидаемый результат |
|-------------|--------|---------------------|
| ✅ Выбран | Нет | 2 чата |
| ☐ Не выбран | Нет | 0 чатов ("Выберите бота") |
| ✅ Выбран | "Важное" | 1 чат (WALEAD) |
| ☐ Не выбран | "Важное" | 0 чатов ("Выберите бота") |

---

## ❌ Если не работает

### Проблема: При снятии чекбокса чаты остаются

**Проверка в консоли:**
1. Снимите чекбокс
2. Ищите строку: `Bot filter toggled. Selected bots:`
3. Должно быть: `[]` (пустой массив)

**Если не пусто** - проблема в handleToggleBotFilter

**Если пусто, но чаты остаются** - ищите строку:
```
No bots selected - showing empty list
```

**Если этой строки нет** - проблема в loadChats

### Проблема: Сообщение не меняется

**Проверка:**
- При selectedBots = [] должно быть: "Выберите хотя бы одного бота"
- При selectedBots = [...] но chats = [] должно быть: "Нет чатов"

---

## 🔧 Как это работает

### Логика в App.js:
```javascript
if (bots.length > 0 && selectedBots.length === 0) {
  console.log('No bots selected - showing empty list');
  setChats([]);
  return; // ← Не делаем запрос к API
}

if (selectedBots.length > 0) {
  params.bot_ids = selectedBots.join(','); // ← Передаем только выбранных
}
```

### Логика в ChatList.js:
```javascript
{chats.length === 0 ? (
  <div className="empty-chats">
    <p>
      {selectedBots.length === 0 
        ? 'Выберите хотя бы одного бота'  // ← Подсказка
        : 'Нет чатов'}
    </p>
  </div>
) : (
  // Список чатов
)}
```

---

**Проверьте все 4 теста и сообщите результаты! 🔍**
