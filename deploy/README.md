# 🚀 Telegram Chat Panel - Развертывание на VPS

Полная инструкция по развертыванию вашего приложения из GitHub на VPS.

---

## 📋 Что вам нужно

✅ VPS с Ubuntu 24.04  
✅ Root доступ к VPS  
✅ IP адрес VPS  
✅ GitHub репозиторий: `raskrytkainsty777-spec/tgvchatbox`

---

## 🎯 Процесс развертывания

### Этап 1: Добавить deployment файлы в GitHub

**Файлы находятся в папке `/app/` на Emergent:**

```
/app/
├── Dockerfile.backend           → В корень репозитория
├── Dockerfile.frontend          → В корень репозитория  
├── docker-compose.prod.yml      → В корень репозитория
├── nginx.conf                   → В корень репозитория
└── deploy/
    ├── install-vps.sh           → В папку deploy/
    └── QUICK_START.md           → В папку deploy/
```

**Как добавить (через GitHub веб-интерфейс):**

1. Откройте https://github.com/raskrytkainsty777-spec/tgvchatbox
2. Нажмите **"Add file"** → **"Create new file"**
3. Создайте каждый файл, скопировав содержимое из Emergent
4. Для файлов в папке `deploy/` сначала напишите `deploy/` в имени файла

> 📖 Подробная инструкция: `ADD_TO_GITHUB.txt`

---

### Этап 2: Развернуть на VPS

После того как файлы добавлены в GitHub:

**Подключитесь к VPS по SSH:**
```bash
ssh root@ВАШ_IP
```

**Выполните 3 команды:**
```bash
# 1. Скачать скрипт установки
curl -o install-vps.sh https://raw.githubusercontent.com/raskrytkainsty777-spec/tgvchatbox/main/deploy/install-vps.sh

# 2. Сделать исполняемым
chmod +x install-vps.sh

# 3. Запустить
./install-vps.sh
```

**Скрипт автоматически:**
- ✅ Установит Docker
- ✅ Клонирует репозиторий
- ✅ Соберет все образы
- ✅ Запустит приложение
- ✅ Создаст администратора

⏱️ **Время: 10-15 минут**

---

### Этап 3: Открыть приложение

Откройте браузер: **http://ВАШ_IP**

**Войдите:**
- Логин: `admin`
- Пароль: `admin123`

⚠️ **Сразу смените пароль!**

---

## 🛠️ Управление

```bash
cd /root/telegram-app

# Статус
docker compose -f docker-compose.prod.yml ps

# Логи
docker compose -f docker-compose.prod.yml logs

# Перезапуск
docker compose -f docker-compose.prod.yml restart

# Остановка
docker compose -f docker-compose.prod.yml down
```

---

## 🔄 Обновление

```bash
cd /root/telegram-app
git pull
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml up -d
```

---

## 📚 Документация

- **QUICK_START.md** - Быстрый старт и команды
- **ADD_TO_GITHUB.txt** - Как добавить файлы в GitHub
- **Dockerfile.backend** - Docker образ backend
- **Dockerfile.frontend** - Docker образ frontend
- **docker-compose.prod.yml** - Оркестрация сервисов
- **nginx.conf** - Reverse proxy конфигурация
- **install-vps.sh** - Автоматический установщик

---

## ⚙️ Требования VPS

| Параметр | Минимум | Рекомендуется |
|----------|---------|---------------|
| RAM      | 2GB     | 4GB           |
| Диск     | 20GB    | 40GB          |
| CPU      | 2 ядра  | 4 ядра        |
| ОС       | Ubuntu 24.04 | Ubuntu 24.04 |

---

## 🆘 Проблемы?

1. **Приложение не открывается**
   ```bash
   docker compose -f docker-compose.prod.yml logs
   ```

2. **Порт 80 занят**
   ```bash
   lsof -i :80
   ```

3. **Нет места**
   ```bash
   docker system prune -a
   ```

4. **Firewall блокирует**
   ```bash
   ufw allow 80/tcp
   ufw allow 22/tcp
   ```

---

## 🎉 Готово!

Ваш Telegram Chat Panel успешно развернут и работает!

**Следующие шаги:**
1. ✅ Смените пароль администратора
2. ✅ Добавьте ваших Telegram ботов
3. ✅ Создайте пользователей с ограниченными правами
4. ✅ Настройте метки и автоответы

---

**Репозиторий:** https://github.com/raskrytkainsty777-spec/tgvchatbox
