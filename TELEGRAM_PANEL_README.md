# Telegram Chat Panel - Панель управления Telegram ботами

## 📋 Описание

Полнофункциональная панель управления для Telegram ботов с современным интерфейсом в стиле Telegram Desktop.

## ✨ Функции

### Управление ботами
- ✅ Добавление ботов через токен (от @BotFather)
- ✅ Просмотр списка всех ботов
- ✅ Удаление ботов
- ✅ Автоматический запуск ботов при старте сервера

### Работа с чатами
- ✅ Просмотр всех диалогов с пользователями
- ✅ Отображение от новых к старым
- ✅ Поиск по чатам (имя, username)
- ✅ Фильтрация по ботам (множественный выбор через чекбоксы)
- ✅ Счетчик непрочитанных сообщений
- ✅ Автоматическая пометка сообщений как прочитанных

### Сообщения
- ✅ Просмотр истории сообщений
- ✅ Отправка текстовых сообщений
- ✅ Отправка файлов (документы, изображения)
- ✅ Эмодзи клавиатура
- ✅ Массовая рассылка сообщений
- ✅ Real-time обновления (автообновление каждые 2-3 секунды)

### Интерфейс
- ✅ Темная тема в стиле Telegram Desktop
- ✅ Двухколоночный layout (чаты слева, сообщения справа)
- ✅ Адаптивный дизайн
- ✅ Статистика в реальном времени

## 🚀 Как использовать

### 1. Создание Telegram бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
4. Сохраните токен - он понадобится для добавления бота в панель

### 2. Добавление бота в панель

1. Откройте приложение в браузере
2. Нажмите на иконку настроек (⚙️) в правом верхнем углу
3. В открывшемся окне вставьте токен бота
4. Нажмите "Добавить бота"
5. Бот появится в списке и начнет получать сообщения

### 3. Работа с чатами

1. Попросите пользователя написать боту в Telegram (команда `/start` или любое сообщение)
2. Чат автоматически появится в списке слева
3. Кликните на чат, чтобы открыть переписку
4. Используйте фильтры для отображения чатов только определенных ботов

### 4. Отправка сообщений

**Одиночное сообщение:**
1. Выберите чат
2. Введите текст в поле внизу
3. Нажмите кнопку отправки или Enter

**С эмодзи:**
1. Нажмите кнопку смайлика (😊)
2. Выберите нужный эмодзи
3. Эмодзи добавится в текст

**Отправка файла:**
1. Нажмите кнопку скрепки (📎)
2. Выберите файл
3. Добавьте текст описания (опционально)
4. Файл отправится автоматически

**Массовая рассылка:**
1. Нажмите кнопку "Группа пользователей" (👥) в header чата
2. Введите ID пользователей через запятую (например: `123456, 789012`)
3. Введите текст сообщения
4. Нажмите "Отправить"

### 5. Получение User ID

Чтобы узнать ID пользователя:
- Откройте любой чат с пользователем
- ID отображается в URL базы данных или можно использовать специального бота @userinfobot в Telegram

## 🔧 Технические детали

### Стек технологий
- **Backend:** FastAPI + Python + python-telegram-bot
- **Frontend:** React + CSS (Telegram Desktop style)
- **Database:** MongoDB
- **Real-time:** Polling (каждые 2-3 секунды)

### API Endpoints

```bash
# Боты
GET    /api/bots              # Список ботов
POST   /api/bots              # Добавить бота
DELETE /api/bots/{bot_id}     # Удалить бота

# Чаты
GET    /api/chats             # Список чатов (с фильтрами)
GET    /api/chats/{chat_id}   # Один чат

# Сообщения
GET    /api/messages/{chat_id}       # История сообщений
POST   /api/messages                 # Отправить сообщение
POST   /api/messages/file            # Отправить файл
POST   /api/messages/broadcast       # Массовая рассылка
PATCH  /api/messages/read            # Пометить как прочитанные

# Статистика
GET    /api/stats             # Общая статистика
```

### Примеры использования API

**Добавить бота:**
```bash
curl -X POST http://localhost:8001/api/bots \
  -H "Content-Type: application/json" \
  -d '{"token": "YOUR_BOT_TOKEN"}'
```

**Получить чаты:**
```bash
curl http://localhost:8001/api/chats
```

**Отправить сообщение:**
```bash
curl -X POST http://localhost:8001/api/messages \
  -H "Content-Type: application/json" \
  -d '{
    "bot_id": "bot-uuid",
    "user_id": 123456789,
    "text": "Привет!"
  }'
```

**Массовая рассылка:**
```bash
curl -X POST http://localhost:8001/api/messages/broadcast \
  -H "Content-Type: application/json" \
  -d '{
    "bot_id": "bot-uuid",
    "user_ids": [123456, 789012],
    "text": "Массовое сообщение!"
  }'
```

## 📊 Структура базы данных

**Коллекция `bots`:**
```javascript
{
  id: "uuid",
  token: "bot_token",
  username: "bot_username",
  first_name: "Bot Name",
  is_active: true,
  created_at: "datetime"
}
```

**Коллекция `chats`:**
```javascript
{
  id: "bot_id_user_id",
  bot_id: "uuid",
  user_id: 123456,
  username: "user_name",
  first_name: "Name",
  last_name: "Surname",
  last_message: "Text",
  last_message_time: "datetime",
  unread_count: 5,
  created_at: "datetime",
  updated_at: "datetime"
}
```

**Коллекция `messages`:**
```javascript
{
  id: "uuid",
  chat_id: "chat_id",
  bot_id: "bot_id",
  user_id: 123456,
  text: "Message text",
  file_id: "telegram_file_id",
  file_type: "photo|document",
  is_from_bot: false,
  is_read: false,
  created_at: "datetime"
}
```

## 🎨 Дизайн

Интерфейс полностью повторяет стиль Telegram Desktop:
- Темная цветовая схема (#0e1621, #17212b)
- Синие акценты (#5288c1)
- Круглые аватары с градиентами
- Плавные анимации
- Характерные шрифты и отступы

## 🔐 Безопасность

- Токены ботов хранятся в базе данных
- Все API запросы проходят через CORS middleware
- Валидация данных через Pydantic
- Обработка ошибок на всех уровнях

## 📝 Заметки

1. Боты автоматически запускаются при старте сервера
2. Сообщения обновляются автоматически каждые 2-3 секунды
3. Чаты сортируются по времени последнего сообщения (новые вверху)
4. Непрочитанные сообщения автоматически помечаются как прочитанные при открытии чата

## 🐛 Troubleshooting

**Бот не получает сообщения:**
- Убедитесь, что токен правильный
- Проверьте, что пользователь написал боту первым (нажал /start)
- Проверьте логи backend: `tail -f /var/log/supervisor/backend.err.log`

**Чаты не отображаются:**
- Обновите страницу
- Проверьте фильтры ботов (галочки должны быть установлены)
- Проверьте подключение к базе данных

**Файлы не отправляются:**
- Проверьте размер файла (Telegram имеет ограничения)
- Проверьте права доступа к /tmp директории
- Проверьте логи backend

## 🎯 Дальнейшее развитие

Возможные улучшения:
- WebSocket для real-time обновлений
- Поддержка стикеров и GIF
- Статистика по ботам
- Экспорт переписки
- Шаблоны сообщений
- Автоответчики
- Анализ активности пользователей

## 📞 Поддержка

Если возникли вопросы или проблемы, проверьте:
1. Логи backend: `/var/log/supervisor/backend.*.log`
2. Логи frontend: `/var/log/supervisor/frontend.*.log`
3. Статус сервисов: `sudo supervisorctl status`

---

**Готово! Панель полностью функциональна и готова к использованию! 🚀**
