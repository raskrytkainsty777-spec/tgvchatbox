# 🚀 Инструкция по развертыванию на VPS

## Требования
- Ubuntu 24.04
- Root доступ
- Минимум 2GB RAM
- IP адрес VPS

## Шаг 1: Скачать архив приложения

На вашем **локальном компьютере**:

1. Скачайте ZIP архив проекта из Emergent
2. Распакуйте архив

## Шаг 2: Загрузить на VPS

### Вариант A: Через SCP (из терминала)

```bash
# Замените YOUR_VPS_IP на ваш IP адрес
scp -r /путь/к/распакованной/папке root@YOUR_VPS_IP:/root/telegram-app
```

### Вариант B: Через FileZilla / WinSCP

1. Подключитесь к VPS через SFTP
2. Загрузите всю папку в `/root/telegram-app`

## Шаг 3: Подключиться к VPS по SSH

```bash
ssh root@YOUR_VPS_IP
```

## Шаг 4: Перейти в папку приложения

```bash
cd /root/telegram-app
```

## Шаг 5: Сделать скрипт исполняемым

```bash
chmod +x install.sh
```

## Шаг 6: Запустить установку

```bash
./install.sh
```

Скрипт спросит IP адрес вашего VPS - введите его и нажмите Enter.

**Установка займет 10-15 минут.**

## Шаг 7: Готово!

После завершения установки вы увидите:

```
✅ Установка завершена!

Ваше приложение доступно по адресу:
👉 http://YOUR_VPS_IP

Данные для входа:
Логин: admin
Пароль: admin123
```

Откройте браузер и перейдите по адресу `http://YOUR_VPS_IP`

---

## 🔧 Полезные команды

### Посмотреть статус контейнеров
```bash
cd /root/telegram-app
docker compose -f docker-compose.prod.yml ps
```

### Посмотреть логи
```bash
# Все логи
docker compose -f docker-compose.prod.yml logs

# Логи backend
docker compose -f docker-compose.prod.yml logs backend

# Логи frontend
docker compose -f docker-compose.prod.yml logs frontend

# Следить за логами в реальном времени
docker compose -f docker-compose.prod.yml logs -f
```

### Перезапустить приложение
```bash
docker compose -f docker-compose.prod.yml restart
```

### Остановить приложение
```bash
docker compose -f docker-compose.prod.yml down
```

### Запустить приложение
```bash
docker compose -f docker-compose.prod.yml up -d
```

### Обновить код приложения
```bash
# Остановить
docker compose -f docker-compose.prod.yml down

# Загрузить новый код (через SCP/SFTP)

# Пересобрать и запустить
docker compose -f docker-compose.prod.yml build
docker compose -f docker-compose.prod.yml up -d
```

---

## ⚠️ Важные замечания

### 1. Firewall
Убедитесь что порты открыты на вашем VPS:
```bash
# Для Ubuntu с ufw
ufw allow 80/tcp
ufw allow 22/tcp
ufw reload
```

### 2. Безопасность

**После первого входа:**
1. Смените пароль администратора
2. Создайте нового пользователя с ограниченными правами
3. Используйте сложные пароли для ботов

### 3. Backup базы данных

```bash
# Создать backup
docker exec telegram_mongodb mongodump --out /data/backup

# Скопировать backup на локальный компьютер
docker cp telegram_mongodb:/data/backup ./mongodb-backup
```

### 4. Мониторинг использования ресурсов

```bash
# Посмотреть использование ресурсов контейнерами
docker stats
```

---

## 🆘 Решение проблем

### Приложение не открывается

1. Проверьте статус контейнеров:
```bash
docker compose -f docker-compose.prod.yml ps
```

2. Посмотрите логи:
```bash
docker compose -f docker-compose.prod.yml logs
```

### Порт уже занят

Если порт 80 уже используется другим приложением, измените в `docker-compose.prod.yml`:
```yaml
nginx:
  ports:
    - "8080:80"  # Измените 80 на 8080
```

Тогда приложение будет доступно на `http://YOUR_VPS_IP:8080`

### Backend не подключается к MongoDB

Проверьте что MongoDB запущен:
```bash
docker compose -f docker-compose.prod.yml logs mongodb
```

### Frontend не может подключиться к Backend

Убедитесь что в `frontend/.env` правильно указан IP:
```
REACT_APP_BACKEND_URL=http://YOUR_VPS_IP:8001
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи контейнеров
2. Убедитесь что все порты открыты
3. Проверьте что на VPS достаточно памяти (минимум 2GB)

---

## 🎉 Поздравляем!

Ваш Telegram Chat Panel успешно развернут на VPS и готов к работе!
