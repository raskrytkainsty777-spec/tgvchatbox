version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: telegram_mongodb
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=telegram_chat_db
    networks:
      - telegram_network

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: telegram_backend
    restart: always
    ports:
      - "8001:8001"
    environment:
      - MONGO_URL=mongodb://mongodb:27017
      - CORS_ORIGINS=http://YOUR_VPS_IP,http://YOUR_VPS_IP:3000
    depends_on:
      - mongodb
    networks:
      - telegram_network
    volumes:
      - ./backend:/app

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: telegram_frontend
    restart: always
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_BACKEND_URL=http://YOUR_VPS_IP:8001
    depends_on:
      - backend
    networks:
      - telegram_network

  nginx:
    image: nginx:alpine
    container_name: telegram_nginx
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - backend
    networks:
      - telegram_network

volumes:
  mongo_data:

networks:
  telegram_network:
    driver: bridge
